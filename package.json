{
  "name": "geospace-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env server.js",
    "build": "vite build",
    "start": "node --env-file=.env server.js --production"
  },
  "dependencies": {
    "katex": "^0.18.4",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
